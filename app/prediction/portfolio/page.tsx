import PredictPortfolio from "@/components/prediction/PredictPortfolio";

export const metadata = { title: "My Portfolio · Deepcast" };

export default function PredictionPortfolioPage() {
  return <PredictPortfolio />;
}
